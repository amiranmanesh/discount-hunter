## What changed

<!-- One or two sentences. -->

## Why

<!-- Link the issue, or describe the behaviour that was wrong. -->

## Checklist

- [ ] `npm run verify` passes (format, lint, typecheck, tests, build)
- [ ] Tests added or updated for anything in `src/core/` or `src/auth/`
- [ ] `docs/API.md` updated if an endpoint or payload shape changed
- [ ] Layout checked with `npm run preview:ui` if the UI moved
- [ ] `CHANGELOG.md` updated under **Unreleased**
- [ ] No price is shown that the signed-in account could not actually pay
